import React from "react";

const Header = props => (
    <div>
  <header role="banner">

  <button className="button start-button">GET STARTED</button>
  </header>
  
  </div>
);

export default Header;
