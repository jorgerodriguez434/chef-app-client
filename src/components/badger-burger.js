import React from "react";
import * as menu from "../menu";

const BadgerBurger = () => (
  <div>
    <h2> Badger Burger</h2>
    <img src={menu.badgerBurger.image} alt="badger-burger" />
  </div>
);

export default BadgerBurger;
